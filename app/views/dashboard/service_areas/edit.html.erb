<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Editing service area</h1>

  <%= render "form", service_area: @service_area %>

  <%= link_to "Show this service area",
  [:dashboard, @service_area],
  class: "btn-secondary" %>
  <%= link_to "Back to service areas",
  dashboard_service_areas_path,
  class: "btn-secondary" %>
</div>
